# ── Website (frontend server) ─────────────────────────────────────────────────
# URL of the API server — all /api/* requests are proxied here.
SILVERSHADE_API=http://127.0.0.1:8000

# Port this frontend server listens on.
PORT=8080

# ── Legacy (old monolith mode — kept for reference, not used by serve.py) ─────
# APP_NAME=SilverShade Control
# SECRET_KEY=...
# MONGODB_URI=...
# SERVICE_API_KEY=...
# These settings now live in API/.env
